{
    "name": "psgmrunner",
    "displayName": "psgmrunner",
    "description": "Preset, target, build, run, and debug workflow for CMake-based C++ projects.",
    "version": "0.0.1",
    "publisher": "local",
    "license": "MIT",
    "engines": {
        "vscode": "^1.85.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onWorkspaceContains:CMakePresets.json"
    ],
    "main": "./out/extension.js",
    "scripts": {
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./"
    },
    "devDependencies": {
        "@types/node": "^20.11.30",
        "@types/vscode": "^1.85.0",
        "typescript": "^5.4.5"
    },
    "contributes": {
        "commands": [
            {
                "command": "psgmrunner.refresh",
                "title": "psgmrunner: Refresh"
            },
            {
                "command": "psgmrunner.selectPreset",
                "title": "psgmrunner: Select Preset"
            },
            {
                "command": "psgmrunner.buildTarget",
                "title": "psgmrunner: Build Target"
            },
            {
                "command": "psgmrunner.runTarget",
                "title": "psgmrunner: Run Target"
            },
            {
                "command": "psgmrunner.debugTarget",
                "title": "psgmrunner: Debug Target"
            }
        ],
        "viewsContainers": {
            "activitybar": [
                {
                    "id": "psgmrunner",
                    "title": "psgmrunner",
                    "icon": "resources/cmake-runner.svg"
                }
            ]
        },
        "views": {
            "psgmrunner": [
                {
                    "id": "psgmrunner.presets",
                    "name": "Presets"
                },
                {
                    "id": "psgmrunner.targets",
                    "name": "Targets"
                }
            ]
        },
        "menus": {
            "view/title": [
                {
                    "command": "psgmrunner.refresh",
                    "when": "view == psgmrunner.presets || view == psgmrunner.targets",
                    "group": "navigation"
                }
            ],
            "view/item/context": [
                {
                    "command": "psgmrunner.selectPreset",
                    "when": "view == psgmrunner.presets && viewItem == preset",
                    "group": "inline@1"
                },
                {
                    "command": "psgmrunner.buildTarget",
                    "when": "view == psgmrunner.targets && viewItem == target",
                    "group": "inline@1"
                },
                {
                    "command": "psgmrunner.runTarget",
                    "when": "view == psgmrunner.targets && viewItem == target",
                    "group": "inline@2"
                },
                {
                    "command": "psgmrunner.debugTarget",
                    "when": "view == psgmrunner.targets && viewItem == target",
                    "group": "inline@3"
                }
            ]
        },
        "configuration": {
            "title": "psgmrunner",
            "properties": {
                "psgmrunner.tasks.buildCommandTemplate": {
                    "type": "string",
                    "default": "cmake --build ${buildDir} --config ${preset} --target ${target}",
                    "description": "Build command template. Supports ${buildDir}, ${preset}, ${target}, and ${sourceDir}."
                },
                "psgmrunner.tasks.runCommandTemplate": {
                    "type": "string",
                    "default": "${buildDir}/${target}",
                    "description": "Run command template. Supports ${buildDir}, ${preset}, ${target}, and ${sourceDir}."
                },
                "psgmrunner.tasks.clearTerminalBeforeRun": {
                    "type": "boolean",
                    "default": true,
                    "description": "Clear the shared terminal before starting a build or run task."
                }
            }
        }
    }
}